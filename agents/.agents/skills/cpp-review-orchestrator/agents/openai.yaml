interface:
  display_name: "C++ Review Orchestrator"
  short_description: "Review C++23 with the CDT++ toolchain"
  default_prompt: "Use $cpp-review-orchestrator to review changed C++23 code with the CMake/vcpkg/doctest/just toolchain, apply fixes only when requested, and run focused validators."
