interface:
  display_name: "Review Graph"
  short_description: "Run complete isolated repository review graphs"
  default_prompt: >-
    Use $review-graph to exhaustively route every applicable repository-review skill, validate exact paths, run fresh-context workers across bounded resumable
    epochs, coalesce validation, close late handoffs, and synthesize the final result.
