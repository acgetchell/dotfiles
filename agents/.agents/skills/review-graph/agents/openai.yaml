interface:
  display_name: "Review Graph"
  short_description: "Run complete auditable repository reviews"
  default_prompt: >-
    Use $review-graph to run every applicable review skill, prefer parallel
    subagents with coordinator fallback, validate through $review-validator,
    and return one verified repository proof.
