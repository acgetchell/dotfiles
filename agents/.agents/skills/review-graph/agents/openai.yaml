interface:
  display_name: "Review Graph"
  short_description: "Run complete auditable repository reviews"
  default_prompt: >-
    Use $review-graph to run a complete auditable repository review with grouped delivery by default and strict isolated workers only when explicitly requested.
