interface:
  display_name: "C++ Test Quality"
  short_description: "Audit C++ tests and diagnostic coverage"
  default_prompt: "Use $cpp-test-quality to audit changed C++ tests, sanitizer coverage, deterministic fixtures, and failure-path assertions."
